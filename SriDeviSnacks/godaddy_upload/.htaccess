RewriteEngine On

# Do not rewrite api folder requests (handled by api/.htaccess)
RewriteRule ^api/ - [L]

# Route all other non-file/non-directory requests to index.html for React Router
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
